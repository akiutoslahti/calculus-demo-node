kind: Service
apiVersion: v1
metadata:
  labels:
    app: aki-calculus
  name: aki-calculus
  namespace: workshop

spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: aki-calculus
